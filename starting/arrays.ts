const superheros: string[] = []

superheros.push("flash")

console.log(superheros);

type Usr = {
  name: string
  isActive: boolean
}

const allUsers: Usr[] = []

allUsers.push({name: "fred", isActive: false})